---
services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: exmeralda_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "4000:4000"
    hostname: exmeralda.local
    networks:
      default:
        aliases:
          - exmeralda.local
    environment:
      DB_HOST: db
      DB_NAME: exmeralda_dev
      DB_USER: postgres
      DB_PASSWORD: postgres
      MIX_ENV: dev
      HOST: "0.0.0.0"
      OLLAMA_HOST: ${OLLAMA_HOST:-http://host.docker.internal:11434}
      RELEASE_COOKIE: exmeralda
    volumes:
      - .:/app
    depends_on:
      db:
        condition: service_healthy
    stdin_open: true
    tty: true
    command: >
      /bin/sh -c "
        echo 'Installing dependencies...' &&
        mix deps.get &&
        mix deps.compile &&
        echo '' &&
        echo 'Compiling application...' &&
        mix compile &&
        echo '' &&
        echo 'Setting up database...' &&
        mix ecto.create &&
        mix ecto.migrate &&
        mix seed &&
        echo '' &&
        echo 'Setting up assets...' &&
        cd assets && npm install && cd .. &&
        echo '' &&
        echo "" &&
        echo '==========================================' &&
        echo 'Exmeralda app is starting...' &&
        echo 'Open http://localhost:4000' &&
        echo '=========================================='
        iex --name exmeralda@exmeralda.local --cookie exmeralda -S mix phx.server
      "

  livebook:
    image: ghcr.io/livebook-dev/livebook:0.17.2
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      LIVEBOOK_COOKIE: exmeralda
      LIVEBOOK_TOKEN_ENABLED: "false"
      LIVEBOOK_DEFAULT_RUNTIME: "attached:exmeralda@exmeralda.local:exmeralda"
    volumes:
      - .:/data
    depends_on:
      - app

  # OLLAMA_HOST=http://ollama:11434 docker compose --profile llm up
  ollama:
    profiles: ["llm"]
    image: ollama/ollama:latest
    volumes:
      - ollama_models:/root/.ollama
    ports:
      - "11434:11434"
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 15s

  model-pull:
    profiles: ["llm"]
    image: ollama/ollama:latest
    volumes:
      - ollama_models:/root/.ollama
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      OLLAMA_HOST: http://ollama:11434
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        echo "=========================================="
        echo "Pulling Ollama models..."
        echo "=========================================="

        echo ""
        echo "[1/3] Pulling llama3.2..."
        ollama pull llama3.2:latest

        echo ""
        echo "[2/3] Pulling jina-embeddings..."
        ollama pull unclemusclez/jina-embeddings-v2-base-code

        echo ""
        echo "[2/3] Pulling gpt-oss:latest..."
        ollama pull gpt-oss:latest

        echo ""
        echo "=========================================="
        echo "All models ready!"
        echo "=========================================="

volumes:
  pgdata:
  ollama_models:
